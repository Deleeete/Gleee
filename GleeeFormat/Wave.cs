using System;
using System.Collections.Generic;
using System.Text;

namespace GleeeFormat
{
    public static class Wave
    {
        //public static byte[] WaveHeader()
        //{
        //    char[] riff = { 'R', 'I', 'F', 'F' };
        //    char[] type = { 'W', 'A', 'V', 'E' };
        //    char[] fmt = { 'f', 'm', 't', ' ' };
        //    char[] data = { 'd', 'a', 't', 'a' };
        //    char[] ChunkRiff = { 'R', 'I', 'F', 'F' };
        //    char[] ChunkType = { 'W', 'A', 'V', 'E' };
        //    char[] ChunkFmt = { 'f', 'm', 't', ' ' };
        //    char[] ChunkData = { 'd', 'a', 't', 'a' };
        //    short pad = 1;                // File padding
        //    int format_chunk_length = 0x10;  // Format chunk length.
        //    int length = 0;                // File length, minus first 8 bytes of RIFF description. This will be filled in later.
        //    short bytes_per_sample = 0;     // Bytes per sample.
        //}
    }
}
